// 账号配置
// 加载配置文件
final Properties properties = new Properties()
InputStream inputStream = project.rootProject.file('local.properties').newDataInputStream()
properties.load(inputStream)

ext {
    // 基本配置信息，签名请替换您自己的签名文件，建议在local.properties里面配置。
    account = [
            // 密钥配置，默认读取根目录下的debug密钥，如需要发布请修改正式的密钥
            storeFile      : properties.getProperty("storeFile"),
            keyAlias       : properties.getProperty("keyAlias"),
            keyPassword    : properties.getProperty("keyPassword"),
            storePassword  : properties.getProperty("storePassword"),
            //
            debug_storeFile      : properties.getProperty("debug_storeFile"),
            debug_keyAlias       : properties.getProperty("debug_keyAlias"),
            debug_keyPassword    : properties.getProperty("debug_keyPassword"),
            debug_storePassword  : properties.getProperty("debug_storePassword"),

            // 友盟
            umengAppKey    : properties.getProperty("umengAppKey", "11"),
            // 腾讯Bugly
            buglyAppId     : properties.getProperty("buglyAppId", "11"),
            // 友盟
            leancloudAppId : properties.getProperty("leancloudAppId", "11"),
            leancloudAppKey: properties.getProperty("leancloudAppKey", "11"),
            // 微信分享
            wechatAppId    : properties.getProperty("wechatAppId", "11"),
            wechatAppSecret: properties.getProperty("wechatAppSecret", "11"),
            // 微博分享
            weiboAppId     : properties.getProperty("weiboAppId", "11"),
            weiboAppSecret : properties.getProperty("weiboAppSecret", "111"),
            // QQ分享
            qqAppId        : properties.getProperty("qqAppId", "111"),
            qqAppSecret    : properties.getProperty("qqAppSecret", "1111"),
            // SDK补丁包上传地址
            uploadUrl      : properties.getProperty("uploadUrl", "替换成你的配置")
    ]
}